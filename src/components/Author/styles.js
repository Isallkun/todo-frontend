import styled from "styled-components";

export const AuthorContainer = styled.span`
  font-size: 1rem;
  display: flex;
  justify-content: center;

  a {
    cursor: pointer;
    margin: 0 0.2rem;
  }
`;
